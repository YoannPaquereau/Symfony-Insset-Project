<div class="btn-group dropleft">
    <button type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Trier par
    </button>
    <div class="dropdown-menu">
        <button class="dropdown-item" type="button">{{ knp_pagination_sortable(products, 'Prix', 'p.price') }}</button>
        <button class="dropdown-item" type="button">{{ knp_pagination_sortable(products, 'Nom', 'p.name') }}</button>
        <button class="dropdown-item" type="button">{{ knp_pagination_sortable(products, 'Disponibilité', 'p.available') }}</button>
    </div>
</div>

<div class="row mt-3">
    {% for product in products %}
        <div class="col-md-3 mt-1">
            <div class="card mb-3">
                <img class="card-img-top img-fluid" src="{{ url }}/{{ product.imagePath }}" alt="Card image cap">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title">{{ product.name}}</h5>
                    <p class="card-text">{{ product.description }}</p>
                    <div class="text-primary" style="font-size: 2rem; font-weight: bold;">{{ product.price }} €</div>
                    <a href="{{ path('addBasket', {'id': product.id}) }}" class="btn btn-success float-right">
                        Ajouter
                    </a>
                </div>
            </div>
        </div>
    {% endfor %}
</div>
<div class="pagination">
    {{ knp_pagination_render(products) }}
</div>
<div class="count">
    {{ products.getTotalItemCount }}
</div>
