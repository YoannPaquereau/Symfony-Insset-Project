{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block stylesheets %}
    <style>
        form {
            width: 20%;
            margin: auto;
            top: 50%;
        }
    </style>
{% endblock %}

{% block body %}
    {{ form_start(formLogin) }}
        {{ form_row(formLogin.username) }}
        {{ form_row(formLogin.password) }}
        {{ form_row(formLogin.submitLogin, {'attr': {'class': 'btn btn-primary btn-lg btn-block'}}) }}
    {{ form_end(formLogin) }}
{% endblock %}